fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
asyncpg>=0.29.0
apscheduler>=3.10.0
yt-dlp>=2026.7.4
yt-dlp-ejs
# PO Token provider plugin -- retrieves tokens from the bgutil-ytdlp-pot-provider
# sidecar (http://127.0.0.1:4416 by default, no config needed) to satisfy
# YouTube's proof-of-origin requirement for actual video-data downloads. Without
# it, format URLs resolve and start streaming fine but get cut off mid-transfer
# with a 403 -- confirmed empirically 2026-08-18.
bgutil-ytdlp-pot-provider>=1.3.1
sse-starlette>=2.1.0
python-multipart>=0.0.32
# Observability: standard OpenTelemetry SDK for metrics + traces, exported via
# plain OTLP to the in-cluster collector (see services/telemetry.py). Kept
# vendor-agnostic on purpose -- works against any OTLP-compliant backend, not
# just this cluster's Mimir/Tempo.
opentelemetry-sdk>=1.44
opentelemetry-exporter-otlp-proto-grpc>=1.44
opentelemetry-instrumentation-fastapi>=0.65b0
opentelemetry-instrumentation-sqlalchemy>=0.65b0
opentelemetry-instrumentation-logging>=0.65b0
# Continuous profiling stopgap -- OTel's own profiling signal is still alpha
# (2026-03) with no usable Python SDK and no backend deployed in this stack,
# so this deliberately isn't pure OTLP. pyroscope-otel tags profile samples
# with the active OTel span so they still correlate with the traces above.
pyroscope-io>=1.2
pyroscope-otel>=1.1
